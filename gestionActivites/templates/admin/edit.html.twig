{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body  %}
    <h1 style="text-align: center ; color: white">Edit User</h1>


    {{ include('admin/_form.html.twig', {'button_label': 'Update'}) }}
<div style="display: flex;">
    <div class="card" style="width: 18rem; flex-grow: 1;">
        <div class="card-body" >
            <h5 class="card-title">Back to the users list</h5>
            <p class="card-text">Contient tous les utilisateurs</p>
            <a href="{{ path('user_animator_index') }}" class="btn btn-warning stretched-link">Click Here</a>
        </div>
    </div>
    {{ include('admin/_delete_form.html.twig') }}
</div>
{% endblock %}
