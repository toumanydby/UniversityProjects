{% extends 'base.html.twig' %}

{% block title %}Edit Activity{% endblock %}

{% block body %}
    <h1 style="color: aliceblue">Edit Activity</h1>

    {{ include('activity/_form.html.twig', {'button_label': 'Update'}) }}

<div style="display: flex;">


    {{ include('activity/_delete_form.html.twig') }}

    <div class="card"   >
        <div class="card-body">
            <h5 class="card-title">Back to list</h5>
            <p class="card-text">Contient les differentes activites</p>
            <a href="{{ path('activity_index') }}" class="btn btn-warning stretched-link">Click Here</a>
        </div>
    </div>
</div>


{% endblock %}
